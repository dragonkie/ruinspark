{{#each tabs}}
    <div class="{{tabCssClass}}" data-group="{{group}}" data-tab="{{id}}">
        <div class="rs-tab-content" data-application-part="{{id}}">

        </div>
    </div>
{{/each}}